{
  "name": "@proxy-platform/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "db:provider": "node ../../scripts/set-db-provider.mjs",
    "prisma:generate": "npm run db:provider && prisma generate --schema prisma/schema.prisma",
    "prisma:migrate": "npm run db:provider && prisma migrate dev --schema prisma/schema.prisma",
    "prisma:push": "npm run db:provider && prisma db push --schema prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.16.1",
    "@proxy-platform/shared": "0.1.0",
    "maxmind": "^5.0.6"
  },
  "devDependencies": {
    "prisma": "^5.16.1"
  }
}
